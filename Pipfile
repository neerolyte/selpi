[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
pyserial = "*"
serial = "*"

[requires]
python_version = "3"

[scripts]
tests = "python -m unittest discover -s tests"
test = "python -m unittest"
