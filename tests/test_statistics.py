from unittest import TestCase, skip

class StatisticsTest(TestCase):
    @skip("todo - confirm set of statistics returned")
    @skip("todo - scales are updated only once")
    @skip("todo - formatting of stats dict")
    def test_get(self):
        pass
